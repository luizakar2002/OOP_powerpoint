# include "Parser.hpp"
# include "Application.hpp"

int main(int argc, char **argv)
{
    // InputVector input(argv + 1, argv + argc);

    // Parser      parser(input);
    // parser.parse();

    Application app;
    app.run();
}